# Nexus feedback work queue (2026-09-13)

Source: Nexus Posts tabs scraped with `tools/nexus_comments.py` (comments since 2025-07, plus stickies), analyzed per mod. Full per-mod reports with quotes: `docs/nexus-feedback-2026-09/report_<modid>.md` (5208 LTBF, 4885 Mod Settings, 4583 In-World Navigation, 4575 Input Loader); raw JSON stays out of the repo (re-scrape with the tool). Game is 2.31; all four mods load on it. Items are ordered by user impact within each repo. Each item is sized for one agent; "Done when" is the acceptance test. Repos: `flight_control` (LTBF, Nexus 5208), `../mod_settings` (4885), `../in_world_navigation` (4583), `../input_loader` (4575).

Conventions for agents: conventional commit prefixes (`feat:`/`fix:` reach the changelog), no em-dashes, build with `cmake -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo -G Ninja && cmake --build build`, run `python tools/check_hashes.py` after touching hooks, do not tag releases (the user tags).

## mod_settings (Nexus 4885, ~150 reporters on "menu missing")

- [ ] **MS-1 Sticky / troubleshooting post for 2.31** (docs, not code). Draft `docs/nexus-sticky.md` for the user to pin: 0.2.21 works on 2.31; required RED4ext 1.29+, ArchiveXL 1.27+, redscript 0.5.31 (0.5.30 breaks it); delete the old plugin folder before updating; Vortex does not deploy redscript's `engine/config/base` and `engine/tools` files; Input Loader is required; Project E3 UI conflicts; where `user.ini` and logs live; Reveurr's clean-reinstall bisect. Evidence: three "not showing" waves, 5 to 15 posts/month still. Done when: file exists and the readme requirements section matches it.
- [x] **MS-2 Confirmation dialog and backup on Reset / Default / F1** (done 2026-09-13, mod_settings 9fa4045 + 4582db6; root cause: Reset restored every mod's defaults; needs in-game check) (~11 reporters, the angriest theme). Add an "Are you sure" step before any global reset, back up `red4ext/plugins/mod_settings/user.ini` to `user.ini.bak` before bulk writes, and add per-mod reset. Done when: F1 and both reset buttons prompt; a backup file appears; per-mod reset restores only that mod's defaults.
- [x] **MS-3 Keep settings for mods absent from a session** (done 2026-09-13, mod_settings e904815; root cause of spontaneous resets: user.ini read with a narrow-codepage fopen while written with the wide path, plus OnBack always rewriting the file; needs in-game check) (4 reporters: settings pruned when a mod is temporarily removed or disabled). Stop deleting entries for unregistered mods on save; only prune on explicit user action. Done when: remove a mod, launch, re-add it, its settings survive.
- [ ] **MS-4 Vortex / MO2 packaging** (~18 reporters: MO2 "packed.reds" deploy error, Vortex marks it redundant). Check the release zip layout against what MO2 expects (everything under `red4ext/plugins/mod_settings/`, no top-level loose files) and add Input Loader (and Codeware if needed) to the Nexus requirements list text in the readme. Done when: a fresh MO2 install of the zip deploys without error (document the test).
- [ ] **MS-5 Linux / Proton thread error 87 and hang with Audioware or Drive an Aerial Vehicle** (post-2.31 cluster). Read `src/red4ext` thread/mutex creation ("use red mutex to try to fix hanging" commit 81c2924), look for a race in plugin load and the "Could not open a thread ... error code: 87" path, add a log line around library load. Done when: a plausible fix or a documented reproduction with the logs users posted.
- [ ] **MS-6 Native Settings UI coexistence** (2022 sticky claims compatible, users say the menu disappears). Verify with both installed; fix or correct the sticky text in MS-1. Done when: verified either way and documented.
- [ ] **MS-7 UI fixes bundle**: mouse wheel scrolls both panes (djoole), remember scroll position / last selection, hide empty category separators, fallback key to reopen the menu after a keybind is lost. Done when: each behavior is observable in game.
- [ ] **MS-8 Release v0.2.22 for 2.31** after MS-2/MS-3 land (user tags; CI uploads to Nexus with changelog).

## in_world_navigation (Nexus 4583)

- [ ] **IWN-1 Sticky text** in `docs/nexus-sticky.md`: works on 2.31, needs the latest Mod Settings, settings live under "Mod Settings" not "Mods", pick walking / driving / both. Covers 31 reporters (missing arrows + packed.reds compile errors). Note: the "3.0.80" request is 2.31's file version, not a newer patch.
- [x] **IWN-2 Toggle keybind and HUD-visibility gating** (done 2026-09-13, in_world_navigation 13751c9: Input Loader keybind default `;` flips the Show arrows setting; Follow HUD visibility hides arrows in photo mode and when the minimap widget or its HUD parents are hidden; needs in-game check) (15 requesters; the top request). Add an Input Loader keybind to toggle arrows and hide them when the HUD is hidden (users patch `this.mmcc.lhud_isVisibleNow` in `packed.reds`; Limited HUD, Simple HUD Toggle, AMM all fail today). Done when: toggle works, arrows follow HUD visibility, setting exposed in Mod Settings.
- [ ] **IWN-3 Opacity and vertical offset sliders** (4 + 1 requesters, user patches exist: MustDmrll vertical offset, guruhoro opacity). Done when: both are Mod Settings sliders and apply live.
- [ ] **IWN-4 Hide arrows during autodrive, photo mode, metro and passenger rides** (uecasm: arrows in midair on the metro; WitaraSP autodrive fork; author promised photo mode hide in July 2025). Done when: none of those states draw arrows.
- [ ] **IWN-5 Per-mode arrow size and a flat / flush style** (4 + 9 requesters; author said flush is "computationally expensive", so ship size first, then prototype flush and measure). Done when: walking size setting ships; flush prototype has a measured frame cost.
- [ ] **IWN-6 Driving FPS loss** (4 reports of 20 to 45% loss at speed). Profile the path update in `Hooks/UpdateNavPath.cpp` and the arrow spawn/despawn; throttle updates by speed or distance. Done when: measured before/after at 300 km/h.
- [ ] **IWN-7 Crash triage**: crash on map open (AnonymousGameZ 2026-08-28), crash on save load with VNS installed (GamerChan09), hash error on 0.1.19 (MootsMods). Check the LoadResRef and UpdateNavPath paths for null handling; add guards and logging. Done when: guards added and `tools/check_hashes.py` still passes.
- [ ] **IWN-8 Release v0.1.23** (tags v0.1.21/v0.1.22 never produced releases) after IWN-2/3/4.

## input_loader (Nexus 4575)

- [ ] **IL-1 Version reporting** already fixed on main (Query reported 0.1.1 since forever; users noticed: "Red4ext log shows Input Loader version 0.1.1"). Done when: v0.2.4 is released and the log shows 0.2.4.
- [x] **IL-2 Corruption guard for the "PRESS [None] TO CONTINUE" crash** (done 2026-09-13, input_loader 9f67034; note the ini is a static CMake-installed file, so the plugin-absent case is uninstall guidance only; needs in-game check) (~35 reporters blame IL for crashes; anygoodname reproduced it: `engine/config/platform/pc/input_loader.ini` present but `r6/cache` xmls or the dll missing). On startup, if the ini points at files that do not exist, regenerate them or remove the ini and log loudly. Done when: deleting the cache xmls and launching no longer breaks input.
- [x] **IL-3 Stop shipping stale vanilla XML copies** (done 2026-09-13, input_loader f92f7dc; bundled 2.30 copies removed, startup warns when a base r6/config xml is not a known vanilla hash; per-patch hash table in src/Main.cpp) (7 reporters; harry20199vn's fix of copying vanilla `r6/config` xmls over the bundled ones). Derive from `r6/config` at startup, detect a base file overwritten by another mod and warn. Done when: the release zip no longer contains `inputContexts.xml`/`inputUserMappings.xml` copies, or they are only used as a fallback with a log line.
- [x] **IL-4 Hold-action merge keys on name instead of action** (done 2026-09-13, input_loader 0340659; confirmed: hold/multitap/repeat/toggle/acceptedEvents are keyed by action, buttonGroup by id; needs in-game check) (LasurDragon, slimejean of Faster Interface; likely behind Photo Mode Backspace and Smart Frames softlock reports). Fix the matching in the merge logic. Done when: a hold action in a mod xml replaces the vanilla one instead of appending.
- [x] **IL-5 First-run regeneration timing** (done 2026-09-13, input_loader a9bc19c; root cause: RED4ext game-state callbacks run after the game reads the options ini, ~50 s after plugin Load, so merging now happens in Main(Load) and on Add(); needs fresh-install first-launch check) ("run the game at least twice"). Find why the first launch after install does not apply the merged xmls (ini written after the game reads it?) and fix ordering. Done when: a fresh install works on the first launch.
- [ ] **IL-6 Troubleshooting doc** in readme: cybercmd must be fully uninstalled (11 commenters), clean uninstall file list, do not delete `r6/cache` wholesale, how to read "Error parsing element attribute" and find the offending mod xml, document `acceptedEvents` and event names, NUMLOCK/keypad keys as hotkeys. Done when: readme has the section and `docs/nexus-sticky.md` exists.
- [ ] **IL-7 Deadzone and axis inversion attributes for stick bindings** (1 request, low priority).
- [ ] **IL-8 Release v0.2.4** after IL-2/IL-4 (user tags).

## flight_control / LTBF (Nexus 5208)

651 comments, 345 top-level posts unanswered since 2025-07-16; roughly 45% are problem reports. Reports and quotes in `docs/nexus-feedback-2026-09/report_5208.md`.

- [ ] **LTBF-1 Sticky / description update for 0.3.18** (docs). Draft `docs/nexus-sticky.md`: 0.3.18 is for 2.31; exact requirement versions (RED4ext 1.29+, ArchiveXL 1.27+, TweakXL 1.11+, redscript 0.5.31+, Input Loader 0.2.3, Mod Settings 0.2.21); Limited HUD works again (description still says incompatible, ~12 reporters); error 126/127 on `fmodstudio.dll` means stale FMOD dlls or a wrong folder name, fix is a clean reinstall; install Input Loader and Mod Settings manually if Vortex fails; RadioExt status in one sentence. Covers themes 1, 5, 8 (about 85 reporters). Done when: file exists and readme matches.
- [x] **LTBF-2 CrystalCoat / customization crash** (analysis + guards done 2026-09-13, flight_control 3413474, see docs/crystalcoat-crash-analysis.md; blacklist via `Vehicle.<record>.flightDisabled` tweak flag; four 1.62-era struct layouts on the path still need checking against 2.31 RTTI; needs in-game check on a Caliburn with CrystalCoat) (~30 reporters, Jul 2025 to Apr 2026; author-acknowledged). Crashes on Caliburn, Aerondight, Hella, Type 66, Emperor, Outlaw, Galena, Alvarado, Riptide Terrier, Trophy Tanishi, quest cars. Dumps: "Detected integer overflow for offset ... dynamicBuffer.cpp(96)" and a null read inside `let_there_be_flight.dll`. v0.3.16 disabled customizations as a workaround (`Hooks/VehicleCustomization.cpp`, commit 7a1a51d); reports continue into 2026, so either users run old builds or the crash has a second path. Reproduce with 0.3.18 on a Caliburn with a CrystalCoat color; if it still crashes, add null guards in the customization and mesh hooks and a per-vehicle blacklist tweak. Done when: reproduction documented, guard or blacklist in place, hash check passes.
- [ ] **LTBF-3 Enter/exit vehicle stutter and HUD reset** (~17 reporters, mostly 2026, bisected to LTBF alone). Profile `Entity_InitializeComponents` and the FlightComponent attach path (`EntityAddComponent.cpp`, thruster mesh spawning) on mount/unmount; check whether the HUD reset comes from the redscript `inkWidget`/HUD extensions re-spawning. Done when: measured hitch before/after and HUD no longer rebuilds on enter/exit.
- [x] **LTBF-4 Flight audio lost after Chipping In, act 2, or races** (done 2026-09-13, 0bcf0d0: dropped the TimeSystem.IsPausedState() term from FlightAudio.GetGameVolume; needs in-game check) (~9 reporters). Users fix it with CET `Game.GetTimeSystem():SetPausedState(false, '')`, so FMOD playback is tied to the time-system pause state; make `FlightAudio` resume when the time system unpauses or ignore pause for the flight bus. Done when: audio returns after a scripted pause without CET.
- [ ] **LTBF-5 Expose promised settings and write a settings guide** (~24 reporters on floaty controls, 8 asking for a guide). Add Mod Settings options for invert lift, Automatic lift factor, turn resistance, and an L3 hold-to-toggle or rebind (L3 cannot be rebound today, ~15 reporters; users patch `preset="invertAxis"` on Flight_Lift). Write `docs/settings-guide.md` explaining each `FlightSettings` value with a keyboard and mouse preset. Done when: options appear in Mod Settings and the guide exists.
- [x] **LTBF-6 Uninstall instructions and uninstall.bat** (done 2026-09-13, a5e19dd: configure_uninstall re-enabled, ships red4ext/plugins/let_there_be_flight_uninstall.bat, readme lists files) (9 reporters). `configure_uninstall()` is commented out in `CMakeLists.txt`; re-enable it or ship a documented file list in the readme. Done when: the release zip contains the uninstall script or the readme lists every file.
- [ ] **LTBF-7 Autodrive cinematic camera broken since 2.3** (~8 reporters). Check the TPP camera hooks (`vehicleTPPCameraComponent_*`, `TPPCameraStats_Update`) for the autodrive camera state and pass it through untouched. Done when: cinematic camera works during autodrive with LTBF installed.
- [ ] **LTBF-8 Vehicle whitelist / unlock mechanic** (top feature request since 2022, 11 + 4). Expose the existing `CanActivate()` configuration path (readme already shows per-vehicle classes) as a Mod Settings list or a tweak-driven whitelist, plus an "owned vehicles only" option. Done when: a user can restrict flight to selected or purchased vehicles without writing redscript.
- [ ] **LTBF-9 Physics glitches** (~14): car launched into space on entry (Voshak), undrivable after landing until flight is toggled (PanPan66), traffic vanishing (StreetKidV2). Reproduce each; the undrivable case suggests state left on the vehicle when flight ends without a proper exit. Done when: each is reproduced and either fixed or documented.
- [ ] **LTBF-10 Cleanup**: `@addField ... already defined` redscript warnings (5 reports; also seen in today's smoke test log), 3 missing localization strings (8LOMU8), archive.xl "target sector has 243 node(s), but the mod expects 242" (battleyurika). Done when: none appear in the logs.
- [ ] **LTBF-11 Compat notes**: Dark Future (`HandleCameraInput` collision in DFVehicleSleepSystem.reds), Project E3 HUD layout, Night City Allies, VNS, Car Modification Shop. Investigate the Dark Future one (a named function collision) and document the rest. Done when: readme has a compatibility section.
- [ ] **LTBF-12 Stage 2 SDK sync** (from CLAUDE.md): merge upstream RED4ext.SDK master into the fork, migrate to API v1, update red_lib, input_loader, mod_settings. Not user-facing; schedule after the items above.

### GitHub issues cross-reference (jackhumbert/flight_control, checked 2026-09-13)

30 open issues. Still applicable, mapped to queue items or added:

- #96 Caliburn colour crash (Aug 2025) -> LTBF-2 (guards in 3413474; close after the in-game Caliburn check).
- #99 Never Fade Away crash, `Vehicle.johnny_car`, access violation at +0x10 in the part-detach path (Dec 2025, v0.3.17) -> LTBF-2 covers `VehicleDetachPart` bounds guards; needs a replay of q108 `protect_car` to confirm. Add `Vehicle.johnny_car` to the documented `flightDisabled` examples.
- #101 turning degraded after flight until re-summon, with video (Feb 2026) -> LTBF-9 (undrivable after landing). Best repro we have for that item.
- #93 cinematic camera cannot be activated (Jul 2025) -> LTBF-7.
- #94 crash on startup with a large load order (Jul 2025) -> LTBF-1 sticky (no repro, likely stale requirements).
- #95 (author) hide the flight input hint when the `VehicleBlockFlight` status effect is active; `DefaultTransition.reds` already gates activation on it, the hint does not check it -> new LTBF-13.
- #89 destroyed vehicles fall through the map (v0.3.5, still open) -> new LTBF-14; likely `RemoveColliders()` in `FlightComponent.reds` leaving the wreck without collision.
- #21 footsteps and crosswalk SFX missing (2022; author noted 2025-07 it may be fixed with #88 / 0.3.10) -> ask on the issue, close if no reply.
- #41 photo mode camera and simulation (2022, touched 2025-07) -> LTBF-9 adjacent; verify on 2.31 with the Codeware `IsPhotoModeActive` gate.

Stale or out of scope, close with a note: #92 Blade Runner Spinner proxy mesh (third-party vehicle mod), #98 flying NPC traffic (feature, matches Nexus request; keep open as enhancement), #74 (2024 preview bug list, mostly fixed), #68 camera-follow mechanic (LTBF-5 mouse-aimed request), #67 crash on new save (2023), #5/#6/#8/#14/#15/#20/#27/#28/#32/#39 (2022 enhancements and compat), author's 2022 bug notes #49/#50/#52/#54/#55/#56 (re-verify each in one session, close what no longer reproduces).

- [ ] **LTBF-13 Hide flight input hint under VehicleBlockFlight** (#95). The hint in `DefaultTransition.reds` should be suppressed when `StatusEffectSystem.ObjectHasStatusEffectWithTag(owner, n"VehicleBlockFlight")`. Done when: no flight hint shows in blocked vehicles (quest cars, Delamain).
- [ ] **LTBF-14 Destroyed vehicle wreck falls through the map** (#89, since v0.3.4). Check whether flight cleanup (`RemoveColliders()` and physics state restore in `FlightComponent.reds`) runs on vehicles that get destroyed while flight was ever active or on NPC vehicles; the detached parts keep collision, the body does not. Done when: a destroyed vehicle stays on the ground with LTBF installed.
